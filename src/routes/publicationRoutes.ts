import { Router } from "express";
// import { createPublication, getPublications } from '../controllers/publicationController';

const router = Router();

// // Rotas de publicações
// router.post('/', createPublication); // Rota para criar uma publicação
// router.get('/', getPublications); // Rota para listar publicações

export default router;
